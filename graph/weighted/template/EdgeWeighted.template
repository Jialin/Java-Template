[
  {
    "type": "int",
    "Type": "Int"
  },
  {
    "type": "boolean",
    "Type": "Boolean"
  }
]
====================================================================================================
package template.graph.weighted;

public interface %Type%EdgeWeighted {

  void add(int fromIdx, int toIdx, %type% weight);

  %type% edgeWeight(int edgeIdx);
}
